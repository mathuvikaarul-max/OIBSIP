import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
df = pd.read_csv("ifood_df.csv")
df

df.info()

df.describe()

df.shape
df.columns

df.head()

df.isnull().sum()

df.drop_duplicates()

df.columns = df.columns.str.strip()
print(df.columns)
df["Total_Spending"] = df[
    ["MntWines", "MntFruits", "MntMeatProducts",
     "MntFishProducts", "MntSweetProducts", "MntGoldProds"]
].sum(axis=1)
df["Total_Purchases"] = df[
    ["NumWebPurchases", "NumCatalogPurchases", "NumStorePurchases"]
].sum(axis=1)

print(df[["Total_Spending", "Total_Purchases"]])
 
 df["Income"].hist(bins=20)
plt.title("Income Distribution")
plt.show()

plt.figure(figsize=(10,6))
sns.heatmap(df.corr(), annot=False)
plt.title("Correlation Heatmap")
plt.show()

X = df[["Income", "Total_Spending", "Total_Purchases"]]
print(X)


from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
print("Original Data:\n")
print(X)

print("\nScaled Data:\n")
print(X_scaled[:5])

from sklearn.cluster import KMeans

wcss = []

for i in range(1, 10):
    kmeans = KMeans(n_clusters=i, random_state=42)
    kmeans.fit(X_scaled)
    wcss.append(kmeans.inertia_)

plt.plot(range(1, 10), wcss, marker='*',ms="10",mfc="orange")
plt.title("Elbow Method")
plt.xlabel("Number of Clusters")
plt.ylabel("WCSS")
plt.show()

import pandas as pd
df = pd.read_csv("ifood_df.csv")
df = df.dropna()
df["Total_Spending"] = df[
    ["MntWines","MntFruits","MntMeatProducts",
     "MntFishProducts","MntSweetProducts","MntGoldProds"]
].sum(axis=1)
df["Total_Purchases"] = df[
    ["NumWebPurchases","NumCatalogPurchases","NumStorePurchases"]
].sum(axis=1)
X = df[["Income", "Total_Spending", "Total_Purchases"]]
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
from sklearn.cluster import KMeans
kmeans = KMeans(n_clusters=4, random_state=42)
df["Cluster"] = kmeans.fit_predict(X_scaled)
print(df[["Income", "Total_Spending", "Total_Purchases", "Cluster"]].head())

import matplotlib.pyplot as plt
plt.figure(figsize=(8,6))
plt.scatter(df["Income"], df["Total_Spending"], c=df["Cluster"])
plt.xlabel("Income")
plt.ylabel("Total Spending")
plt.title("Customer Segmentation")
plt.show()

df.groupby("Cluster")[["Income", "Total_Spending", "Total_Purchases"]].mean()

df["Cluster"].value_counts()

df.groupby("Cluster")["Total_Spending"].mean().plot(kind="bar",color="orange")
plt.title("Average Spending per Cluster")
plt.show()
